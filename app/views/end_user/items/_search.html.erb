<%= form_tag items_path, method: :get, class: 'items__searchForm' do %>
  <%= select_tag :artist_id,
                 options_from_collection_for_select(Artist.all, :id, :name),
                 {
                   prompt: 'アーティストで絞り込み',
                   class: 'form-control items__select',
                   onchange: 'submit(this.form);'
                 }
  %>
<% end %>
